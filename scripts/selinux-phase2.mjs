import fs from 'node:fs';
const p='C:/Users/Administrator/Documents/Dev/Rust/SideWire-KernelSU/apps/sidewired/src/main.rs';
let s=fs.readFileSync(p,'utf8');
const old=`fn apply_identity(command: &mut Command, identity: ExecIdentity) -> Result<()> {\n    if matches!(identity, ExecIdentity::Root) {\n        return Ok(());\n    }\n    #[cfg(unix)]\n    {\n        unsafe {\n            command.pre_exec(|| {\n                let groups: [libc::gid_t; 1] = [2000];\n                if libc::setgroups(groups.len(), groups.as_ptr()) != 0 {\n                    return Err(std::io::Error::last_os_error());\n                }\n                if libc::setgid(2000) != 0 {\n                    return Err(std::io::Error::last_os_error());\n                }\n                if libc::setuid(2000) != 0 {\n                    return Err(std::io::Error::last_os_error());\n                }\n                libc::prctl(libc::PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0);\n                Ok(())\n            });\n        }\n    }\n    Ok(())\n}`;
const neu=`fn apply_identity(command: &mut Command, identity: ExecIdentity) -> Result<()> {\n    #[cfg(unix)]\n    unsafe {\n        command.pre_exec(move || {\n            let context = match identity { ExecIdentity::Root => "u:r:ksu:s0", ExecIdentity::Shell => "u:r:shell:s0" };\n            set_exec_context(context)?;\n            if matches!(identity, ExecIdentity::Shell) {\n                const GROUPS: [libc::gid_t; 12] = [1007,1011,1015,1028,1078,1079,2000,3001,3002,3003,3006,3009];\n                if libc::setgroups(GROUPS.len(), GROUPS.as_ptr()) != 0 { return Err(std::io::Error::last_os_error()); }\n                if libc::setgid(2000) != 0 { return Err(std::io::Error::last_os_error()); }\n                if libc::setuid(2000) != 0 { return Err(std::io::Error::last_os_error()); }\n                if libc::prctl(libc::PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0) != 0 { return Err(std::io::Error::last_os_error()); }\n            }\n            Ok(())\n        });\n    }\n    Ok(())\n}\n\n#[cfg(unix)]\nfn set_exec_context(context: &str) -> std::io::Result<()> {\n    let value=CString::new(context).map_err(|_| std::io::Error::new(std::io::ErrorKind::InvalidInput,"invalid SELinux context"))?;\n    let fd=unsafe { libc::open(c"/proc/self/attr/exec".as_ptr(), libc::O_WRONLY | libc::O_CLOEXEC) };\n    if fd < 0 { return Err(std::io::Error::last_os_error()); }\n    let bytes=value.as_bytes_with_nul();\n    let rc=unsafe { libc::write(fd, bytes.as_ptr().cast(), bytes.len()-1) };\n    let close_rc=unsafe { libc::close(fd) };\n    if rc < 0 { return Err(std::io::Error::last_os_error()); }\n    if close_rc != 0 { return Err(std::io::Error::last_os_error()); }\n    Ok(())\n}`;
if(!s.includes(old))throw new Error('identity block missing');
s=s.replace(old,neu);
fs.writeFileSync(p,s);

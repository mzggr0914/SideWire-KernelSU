import fs from 'node:fs';
const p='apps/sidewire/src/main.rs';
let s=fs.readFileSync(p,'utf8');
s=s.replace('    ExecExit, ExecIdentity, ExecRequest, FrameKind, Hello, HelloAck, decode, frame, read_frame,\n    write_frame,','    ExecExit, ExecIdentity, ExecRequest, FileMeta, FilePullRequest, FilePushRequest, FrameKind,\n    Hello, HelloAck, ProxyStartAck, ProxyStartRequest, ProxyTokenMode, decode, frame, raw_frame,\n    read_frame, write_frame,');
s=s.replace('    io::{self, Write},\n    sync::Arc,','    io::{self, Write},\n    net::IpAddr,\n    path::PathBuf,\n    sync::Arc,');
s=s.replace('use tokio::{\n    io::{AsyncBufReadExt, AsyncWriteExt, BufReader},','use tokio::{\n    fs::File,\n    io::{AsyncBufReadExt, AsyncReadExt, AsyncWriteExt, BufReader},');
const end='    Shell {\n        #[arg(long, default_value = DEFAULT_CONTROL)]\n        control: String,\n        #[arg(short = \'s\', long)]\n        device: Option<String>,\n        #[arg(long = "as", value_enum, default_value = "root")]\n        run_as: RunAs,\n    },\n}';
const add=`    Shell {\n        #[arg(long, default_value = DEFAULT_CONTROL)] control: String,\n        #[arg(short = 's', long)] device: Option<String>,\n        #[arg(long = "as", value_enum, default_value = "root")] run_as: RunAs,\n    },\n    Push { #[arg(long, default_value = DEFAULT_CONTROL)] control: String, #[arg(short = 's', long)] device: Option<String>, local: PathBuf, remote: String },\n    Pull { #[arg(long, default_value = DEFAULT_CONTROL)] control: String, #[arg(short = 's', long)] device: Option<String>, remote: String, local: PathBuf },\n    Install { #[arg(long, default_value = DEFAULT_CONTROL)] control: String, #[arg(short = 's', long)] device: Option<String>, apk: PathBuf },\n    Uninstall { #[arg(long, default_value = DEFAULT_CONTROL)] control: String, #[arg(short = 's', long)] device: Option<String>, package: String },\n    Logcat { #[arg(long, default_value = DEFAULT_CONTROL)] control: String, #[arg(short = 's', long)] device: Option<String>, #[arg(long)] clear: bool, args: Vec<String> },\n    Forward { #[arg(long, default_value = DEFAULT_CONTROL)] control: String, #[arg(short = 's', long)] device: Option<String>, local: String, remote: String },\n    Reverse { #[arg(long, default_value = DEFAULT_CONTROL)] control: String, #[arg(short = 's', long)] device: Option<String>, remote: String, local: String },\n    Reboot { #[arg(long, default_value = DEFAULT_CONTROL)] control: String, #[arg(short = 's', long)] device: Option<String>, target: Option<String> },\n    Screencap { #[arg(long, default_value = DEFAULT_CONTROL)] control: String, #[arg(short = 's', long)] device: Option<String>, output: PathBuf },\n    Packages { #[arg(long, default_value = DEFAULT_CONTROL)] control: String, #[arg(short = 's', long)] device: Option<String>, filter: Option<String> },\n    App { #[arg(long, default_value = DEFAULT_CONTROL)] control: String, #[arg(short = 's', long)] device: Option<String>, #[command(subcommand)] command: AppCommand },\n}\n\n#[derive(Subcommand)]\nenum AppCommand {\n    Start { package: String },\n    Stop { package: String },\n    Clear { package: String },\n}`;
if(!s.includes(end)) throw new Error('command enum marker missing');
s=s.replace(end,add);
fs.writeFileSync(p,s);
